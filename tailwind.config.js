module.exports = {
    purge: ["./src/**/*.((t|j)sx?|html)"],
    darkMode: "media", // or 'media' or 'class'
    theme: {
        extend: {},
    },
    variants: {
        extend: {},
    },
    plugins: [],
};
